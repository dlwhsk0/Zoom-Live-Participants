# Entity 문서

이 디렉토리는 현재 프로젝트에서 다루는 저장 엔티티를 정의하는 문서 모음이다.

현재 문서:

- `zoom-raw-webhook-entity.md`
- `participant-event-entity.md`

기준:

- Zoom이 보내는 raw payload는 raw 기준으로 보존
- 서비스에서 조회/가공/Slack 연동에 쓰는 값은 별도 도메인 엔티티로 정의
