{
  "name": "zoom-live-participants",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:schema": "pnpm --filter @zoom-live-participants/api run check:schema"
  },
  "engines": {
    "node": ">=20"
  }
}
